# Environment configurations (Copy to .env and change values)

# Secret keys for signing cookies and JWT session tokens (Change to long random values in production)
SECRET_KEY=your_flask_secret_key_here
JWT_SECRET_KEY=your_jwt_secret_key_here

# Flask port configuration
PORT=5000
